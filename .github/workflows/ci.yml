name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  # build:
  #   name: Build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-toolchain@stable
  #     - uses: Swatinem/rust-cache@v2
  #     - run: cargo build --workspace

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace

  # clippy:
  #   name: Clippy
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-toolchain@stable
  #       with:
  #         components: clippy
  #     - uses: Swatinem/rust-cache@v2
  #     - run: cargo clippy --workspace -- -D warnings

  # fmt:
  #   name: Format
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: dtolnay/rust-toolchain@stable
  #       with:
  #         components: rustfmt
  #     - run: cargo fmt --all -- --check

  # spec-check:
  #   name: Spec Check
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Validate specs directory
  #       run: |
  #         echo "Checking specs/ directory..."
  #         if [ ! -d specs ]; then
  #           echo "::error::specs/ directory not found"
  #           exit 1
  #         fi
  #         count=$(find specs -name '*.md' | wc -l)
  #         echo "Found $count spec files:"
  #         find specs -name '*.md' -print
  #         if [ "$count" -lt 3 ]; then
  #           echo "::error::Expected at least 3 .md files in specs/, found $count"
  #           exit 1
  #         fi
  #         echo "✓ Spec check passed: $count spec files found"
